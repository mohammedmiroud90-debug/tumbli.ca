import { ContentHeader } from "@/components/content-header";
import { JournalFooter } from "@/components/journal-footer";
import { ProjectTracker } from "@/components/project-tracker";
export default async function TrackPage({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; return <main className="min-h-screen bg-[#f5f6f8] text-[#252525]"><ContentHeader locale={locale}/><section className="mx-auto grid max-w-5xl gap-10 px-4 py-14 sm:px-8 sm:py-20 lg:grid-cols-2"><div><p className="text-[11px] font-semibold uppercase tracking-[.18em] text-[#8c1515]">Tumbli Studio</p><h1 className="mt-4 text-5xl font-semibold leading-[1.02] tracking-[-.055em]">Track your project.</h1><p className="mt-6 text-lg leading-8 text-zinc-600">Enter the unique code provided after your website request. You can also sign in to see every request in your dashboard.</p><a href={`/${locale}/login`} className="mt-6 inline-block text-sm font-semibold text-[#8c1515] underline underline-offset-4">Sign in to my account</a></div><ProjectTracker/></section><JournalFooter locale={locale}/></main>; }
