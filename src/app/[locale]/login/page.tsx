import { ContentHeader } from "@/components/content-header";
import { JournalFooter } from "@/components/journal-footer";
import { AccountAccess } from "@/components/account-access";
export default async function LoginPage({ params, searchParams }: { params: Promise<{ locale: string }>; searchParams: Promise<{ next?: string }> }) { const { locale } = await params; const { next } = await searchParams; const nextPath = typeof next === "string" && next.startsWith(`/${locale}/`) ? next : `/${locale}/dashboard`; return <main className="min-h-screen bg-[#f5f6f8] text-[#252525]"><ContentHeader locale={locale}/><section className="mx-auto grid max-w-5xl gap-10 px-4 py-14 sm:px-8 sm:py-20 lg:grid-cols-2"><div><p className="text-[11px] font-semibold uppercase tracking-[.18em] text-[#8c1515]">Tumbli account</p><h1 className="mt-4 text-5xl font-semibold leading-[1.02] tracking-[-.055em]">Your project, all in one place.</h1><p className="mt-6 max-w-md text-lg leading-8 text-zinc-600">Create an account to send a website request and see its progress.</p></div><AccountAccess locale={locale} nextPath={nextPath}/></section><JournalFooter locale={locale}/></main>; }
